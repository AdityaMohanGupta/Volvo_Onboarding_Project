{
  "name": "onbording-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:components": "rm -rf dist-components && LIB_ENTRY=employee-form vite build --config vite.lib.config.ts && LIB_ENTRY=employee-table vite build --config vite.lib.config.ts",
    "preview": "vite preview"
  },
  "dependencies": {
    "lit": "^3.3.3"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.2.0"
  }
}
